const canvas=document.querySelector('#sand'),ctx=canvas.getContext('2d');let W,H,mouse={x:-999,y:-999},particles=[],soundOn=false,audio;
function fit(){W=canvas.width=innerWidth*devicePixelRatio;H=canvas.height=innerHeight*devicePixelRatio;canvas.style.width=innerWidth+'px';canvas.style.height=innerHeight+'px';ctx.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0);const n=Math.min(1250,Math.max(680,Math.floor(innerWidth*innerHeight/1250)));particles=Array.from({length:n},()=>({x:Math.random()*innerWidth,y:Math.random()*innerHeight,vx:(Math.random()-.5)*.19,vy:(Math.random()-.5)*.19,s:Math.random()*1.7+.28,phase:Math.random()*6.28}));}
function draw(t){ctx.clearRect(0,0,innerWidth,innerHeight);for(const p of particles){const dx=p.x-mouse.x,dy=p.y-mouse.y,d=Math.hypot(dx,dy)||1,range=220;if(d<range){const f=(1-d/range)*.18;p.vx+=dx/d*f;p.vy+=dy/d*f}p.vx+=Math.sin(t*.00031+p.phase)*.0027;p.vy+=Math.cos(t*.00024+p.phase)*.0025;p.vx*=.979;p.vy*=.979;p.x+=p.vx;p.y+=p.vy;if(p.x<0||p.x>innerWidth)p.vx*=-1;if(p.y<0||p.y>innerHeight)p.vy*=-1;ctx.fillStyle=`rgba(242,244,245,${.248+Math.sin(t*.0011+p.phase)*.108})`;ctx.fillRect(p.x,p.y,p.s,p.s)}requestAnimationFrame(draw)}
fit();addEventListener('resize',fit);addEventListener('pointermove',e=>{mouse.x=e.clientX;mouse.y=e.clientY});addEventListener('pointerleave',()=>{mouse.x=-999;mouse.y=-999});requestAnimationFrame(draw);
function clickSound(){if(!soundOn)return;audio??=new (window.AudioContext||window.webkitAudioContext)();const osc=audio.createOscillator(),gain=audio.createGain();osc.type='triangle';osc.frequency.setValueAtTime(440,audio.currentTime);osc.frequency.exponentialRampToValueAtTime(180,audio.currentTime+.09);gain.gain.setValueAtTime(.018,audio.currentTime);gain.gain.exponentialRampToValueAtTime(.001,audio.currentTime+.1);osc.connect(gain).connect(audio.destination);osc.start();osc.stop(audio.currentTime+.11)}
document.querySelector('[data-sound]').addEventListener('click',e=>{soundOn=!soundOn;e.currentTarget.setAttribute('aria-pressed',soundOn);clickSound()});document.querySelectorAll('[data-click]').forEach(a=>a.addEventListener('click',clickSound));const reveals=document.querySelectorAll('.reveal');const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting)e.target.classList.add('in')}),{threshold:.18});reveals.forEach(x=>io.observe(x));
